#ifndef STRING_UTILS_HPP
#define STRING_UTILS_HPP

#include <string>

using namespace std;

string trim(string s);
bool separuj(string& linia, char separator, string& wynik); //przyjmuje znak ';' lub ',' - separator

#endif
